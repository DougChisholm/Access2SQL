@page
@model SchoolTests.Web.Pages.Tests.CreateModel
@{
    ViewData["Title"] = "Add Test";
}

<div class="page-wrapper">
    <div class="breadcrumb">
        <a asp-page="/Index">Home</a>
        <span class="breadcrumb-sep">›</span>
        <a asp-page="Index">Tests</a>
        <span class="breadcrumb-sep">›</span>
        <span>Add Test</span>
    </div>

    <div class="page-header">
        <h1 class="page-title">➕ Add Test</h1>
    </div>

    <div class="card" style="max-width:560px;">
        <div class="card-header">
            <span class="card-title">Test Details</span>
        </div>
        <form method="post">
            <div class="card-body">
                <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>
                <div class="form-group">
                    <label asp-for="Test.TestDescription" class="form-label">
                        Test Description <span style="color:var(--c-danger)">*</span>
                    </label>
                    <input asp-for="Test.TestDescription" class="form-control" autofocus
                           placeholder="e.g. Mathematics — Chapter 1: Algebra" />
                    <span asp-validation-for="Test.TestDescription" class="field-validation-error"></span>
                    <p class="form-text">A clear description helps teachers identify the test.</p>
                </div>
            </div>
            <div class="card-footer">
                <button type="submit" class="btn btn-primary">Save Test</button>
                <a asp-page="Index" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
