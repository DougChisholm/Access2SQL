@page "{id:int}"
@model SchoolTests.Web.Pages.Students.EditModel
@{
    ViewData["Title"] = "Edit Student";
}

<div class="page-wrapper">
    <div class="breadcrumb">
        <a asp-page="/Index">Home</a>
        <span class="breadcrumb-sep">›</span>
        <a asp-page="Index">Students</a>
        <span class="breadcrumb-sep">›</span>
        <span>Edit Student</span>
    </div>

    <div class="page-header">
        <h1 class="page-title">✏️ Edit Student</h1>
    </div>

    <div class="card" style="max-width:480px;">
        <div class="card-header">
            <span class="card-title">Update Details</span>
        </div>
        <form method="post">
            <div class="card-body">
                <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>
                <input type="hidden" asp-for="Student.StudentId" />
                <div class="form-group">
                    <label asp-for="Student.StudentName" class="form-label">
                        Student Name <span style="color:var(--c-danger)">*</span>
                    </label>
                    <input asp-for="Student.StudentName" class="form-control" autofocus />
                    <span asp-validation-for="Student.StudentName" class="field-validation-error"></span>
                </div>
            </div>
            <div class="card-footer">
                <button type="submit" class="btn btn-primary">Save Changes</button>
                <a asp-page="Index" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
