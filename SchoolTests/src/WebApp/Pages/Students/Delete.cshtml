@page "{id:int}"
@model SchoolTests.Web.Pages.Students.DeleteModel
@{
    ViewData["Title"] = "Delete Student";
}

<div class="page-wrapper">
    <div class="breadcrumb">
        <a asp-page="/Index">Home</a>
        <span class="breadcrumb-sep">›</span>
        <a asp-page="Index">Students</a>
        <span class="breadcrumb-sep">›</span>
        <span>Delete Student</span>
    </div>

    <div class="page-header">
        <h1 class="page-title">🗑️ Delete Student</h1>
    </div>

    <div class="card" style="max-width:520px;">
        <div class="card-header">
            <span class="card-title">Confirm Deletion</span>
        </div>
        <div class="card-body">
            <div class="alert alert-danger">
                ⚠️ This action cannot be undone. All test results for this student will also be deleted.
            </div>
            <p><strong>Student:</strong> @Model.Student.StudentName</p>
            <p><strong>Results on record:</strong> @Model.ResultCount</p>
        </div>
        <div class="card-footer">
            <form method="post">
                <button type="submit" class="btn btn-danger">🗑️ Yes, Delete</button>
            </form>
            <a asp-page="Index" class="btn btn-secondary">Cancel</a>
        </div>
    </div>
</div>
